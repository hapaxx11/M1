# This workflow is intentionally disabled.
# GitHub Pages deployment is handled by docs.yml which combines
# the web updater and Doxygen API documentation into a single site.
name: GitHub Pages (see docs.yml)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Info
        run: echo "Pages deployment is handled by docs.yml"

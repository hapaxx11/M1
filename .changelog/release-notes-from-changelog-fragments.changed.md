**CI: Release notes now come from changelog fragments** — the "What's Changed"
  section of the GitHub release page is now rendered from the assembled
  `.changelog/*.md` fragments instead of GitHub's PR-label-based
  auto-generated notes, which previously collapsed everything into a single
  "Other Changes" bullet. A "Full Changelog" compare link is still included.

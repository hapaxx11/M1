Documentation: added agent instructions requiring evidence from this repository
(a cited file and line) before removing or disabling any existing capability —
research-agent and web summaries are hypotheses, not evidence — and requiring
that design problems be solved with automatically-correct behaviour instead of
a user-facing setting. The Sub-GHz skill now records that `Bresser 5in1`/`6in1`
are FSK weather protocols, so the Weather Station scene must keep cycling AM and
FSK automatically.

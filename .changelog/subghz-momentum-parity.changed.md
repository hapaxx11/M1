Sub-GHz Read Raw scenes now use the smaller NokiaSmallPlain font for the status bar and RSSI label, matching Momentum style and eliminating text overlap during recording. Waveform graph fills more densely. Decode results filter out single-hit false-positive protocols, keeping only confirmed (repeated) matches. Save from decode now shows inline feedback instead of a separate confirmation screen.
